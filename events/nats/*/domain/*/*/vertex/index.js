import { events as child0Events, meta as child0Meta } from './componentInstance/index.js'
import { events as child1Events, meta as child1Meta } from './stateMachine/index.js'

export const events = {
  "componentInstance": child0Events,
  "stateMachine": child1Events,
}

export const meta = {
  "componentInstance": child0Meta,
  "stateMachine": child1Meta,
}
