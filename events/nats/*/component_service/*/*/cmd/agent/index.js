import { events as child0Events, meta as child0Meta } from './register_components/index.js'

export const events = {
  "register_components": child0Events,
}

export const meta = {
  "register_components": child0Meta,
}
